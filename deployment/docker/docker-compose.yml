services:
  cleanlog:
    build:
      context: ..
      dockerfile: deployment/docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - ENVIRONMENT=local
